{
  "name": "@agentverus/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for AgentVerus — AI agent security scanning as MCP tools",
  "type": "module",
  "bin": {
    "agentverus-mcp-server": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "security", "agent", "scanner", "agentverus", "trust"],
  "author": "AgentVerus",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/agentverus/mcp-server"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "agentverus-scanner": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
